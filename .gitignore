node_modules/
.next/
.vercel/
# env files can hold real API keys — ignore every variant except the template
.env
.env.*
!.env.example
next-env.d.ts
*.tsbuildinfo
.DS_Store
.gstack/
.grabs/
.vercel
# local session store (filesystem persistence — dev only, per-machine data)
.zclip-data/
# owner-only Spec Lab (A/B spec-version arena) — never ships in the
# open-source repo; the whole feature lives in these local-only folders
/app/lab/
/lab/
